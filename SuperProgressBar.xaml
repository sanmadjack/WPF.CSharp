<UserControl x:Class="SMJ.WPF.SuperProgressBar"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             d:DesignHeight="55" d:DesignWidth="514" Background="Transparent">
<!--    <UserControl.Resources>
        <LinearGradientBrush x:Name="ProgressColor" x:Key="ProgressColor" StartPoint="0.5,0" EndPoint="0.5,1">
            <GradientStopCollection>
                <GradientStop Color="#204a87" Offset="0" />
                <GradientStop Color="#204a87" Offset="0.5" />
                <GradientStop Color="#204a87" Offset="1" />
            </GradientStopCollection>
        </LinearGradientBrush>
        <Style x:Key="ProgressBarStyle" TargetType="ProgressBar">
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="ProgressBar">
                        <Grid HorizontalAlignment="Stretch" VerticalAlignment="Stretch">
                            <Rectangle HorizontalAlignment="Stretch" VerticalAlignment="Stretch" RadiusX="5" RadiusY="5" Fill="#2e3436" Opacity="0.25">
                            </Rectangle>
                            <Grid x:Name="PART_Track" Margin="2" >
                                <Rectangle x:Name="PART_Indicator" HorizontalAlignment="Left" RadiusX="5" RadiusY="5" Fill="{DynamicResource ProgressColor}">
                                </Rectangle>
                            </Grid>
                        </Grid>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

    </UserControl.Resources>-->
    <Grid>        
        <ProgressBar Name="progress" Value="50" />
        <Label Content="YAY!" VerticalAlignment="Center" HorizontalAlignment="Center" Name="status" FontSize="20" Foreground="Black"  />
    </Grid>
</UserControl>
